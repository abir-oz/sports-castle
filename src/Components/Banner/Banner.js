import React from 'react';
import './Banner.css'




const Banner = (props) => {
    return (
        <>
            {
                props.children
            }
        </>
    );
};

export default Banner;